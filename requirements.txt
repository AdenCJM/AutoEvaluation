# Core dependency
pyyaml>=6.0.3

# Install ONE provider SDK based on your choice during setup:
#   pip install google-genai      # Gemini
#   pip install openai            # OpenAI
#   pip install anthropic         # Anthropic
